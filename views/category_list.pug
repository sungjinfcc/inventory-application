extends layout

block content
  h1= title
  a(href='/catalog/category/create') Create new category

  ul
    each category in category_list
      li
        a(href=category.url) #{category.name}

    else
      li There are no categorys.
